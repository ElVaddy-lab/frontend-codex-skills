interface:
  display_name: "Frontend Code Architecture"
  short_description: "Structure frontend code into maintainable modules."
  default_prompt: "Use $frontend-code-architecture to organize this frontend code with maintainable component boundaries, file structure, hooks, utilities, data/config separation, styling tokens, and safe refactor guardrails. Preserve existing behavior and verify the result."
