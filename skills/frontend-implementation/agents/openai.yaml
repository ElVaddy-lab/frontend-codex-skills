interface:
  display_name: "Frontend Implementation"
  short_description: "Implement verified frontends from approved designs or plans."
  default_prompt: "Implement the supplied frontend design or approved development plan as a working project. Preserve the handoff contract, follow the selected stack, create complete files and components, remove placeholders, add responsive styling and interactions, then run build, browser QA, and completion gate checks."
